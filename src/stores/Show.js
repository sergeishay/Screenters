import { observable, action, computed } from 'mobx'

export class Show{
    @observable startTime 
    @observable endTime 
    @observable showID
    @observable eventID


    @action updateShow(){
        
    }

}